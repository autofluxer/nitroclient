/* Jungle photo background is handled in CSS (.jungle-bg). Keep stub so script tag is safe. */
(function () {
  const canvas = document.getElementById('nitroBg');
  if (canvas) canvas.style.display = 'none';
})();
